# Setu Docker Environment Configuration

# Validator Configuration
VALIDATOR_ID=validator-1
VALIDATOR_HTTP_PORT=8080
VALIDATOR_LISTEN_ADDR=0.0.0.0
VALIDATOR_P2P_PORT=8081

# Solver Configuration
SOLVER_ID=solver-1
SOLVER_PORT=9001
SOLVER_LISTEN_ADDR=0.0.0.0
SOLVER_CAPACITY=100
AUTO_REGISTER=true

# Network Configuration
VALIDATOR_ADDRESS=validator

# Logging
RUST_LOG=info,setu_validator=debug,setu_solver=debug,consensus=debug

# Storage Paths (inside container)
DB_PATH=/data/db
LOG_PATH=/data/log